package com.alex.cga

import figure.plain.{ConvexPolygon, Point}

package object algorithm {
  implicit val binaryTest = (figure1: Point, figure2: ConvexPolygon) => {
    import Point._
    val a = Point(1,2)
    On
  }
}
